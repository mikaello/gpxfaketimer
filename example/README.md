# Demo - gpxfaketimer

Start the development server:

```shell
npm install
npm run dev
```

You can now open [http://localhost:5173](http://localhost:5173) and start
testing gpxfaketimer.

To build for production:

```shell
npm run build
npm run preview
```
